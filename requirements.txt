pygame>=2.5.0
openai>=1.0.0
# Satellite tracker mode: TLE orbit propagation (pure Python, no key needed).
sgp4>=2.20
requests>=2.28
# Raspberry Pi rotary-encoder input only (--input rotary).
# Install separately on Pi: pip install gpiozero  (or apt install python3-gpiozero)
gpiozero>=2.0; platform_machine=='aarch64' or platform_machine=='armv7l'
